Notes:

(*) Need to build and install libcommni.so libffw.so before

To build NDEB source code use:
$ make all

To clear binary files use:
$ make clean

To clear all output files use:
$ make clean-all
